# 1. Поработайте с переменными, создайте несколько, выведите на экран, запросите у пользователя несколько чисел и строк и сохраните в переменные, выведите на экран.

a = int(10)
b = int(25)
c = a + b

print(" a=", a, "\n", "b=", b, "\n", a, "+", b, "=", c)

firstname_lastname = input("Введите Ваши имя и фамилию: ")
age = int(input("Введите Ваш возраст: "))
work_experience = int(input("Введите Ваш опыт работы (в годах): "))
name_of_the_organization = input("Введите наименование организации: ")

print(" Имя, фамилия:", firstname_lastname, "\n", "Возраст:", age, "лет", "\n", "Опыт работы:", work_experience, "лет", "в организации", name_of_the_organization)

# 2. Пользователь вводит время в секундах. Переведите время в часы, минуты и секунды и выведите в формате чч:мм:сс. Используйте форматирование строк.

chislo = int(input("Введите время в секундах: "))
h = chislo // 3600
m = (chislo // 60) % 60
s = chislo % 60
if m < 10:
    m = str('0' + str(m))
else:
    m = str(m)
if s < 10:
    s = str('0' + str(s))
else:
    s = str(s)
print(str(h) + ':' + str(m) + ':' + str(s))

# 3. Узнайте у пользователя число n. Найдите сумму чисел n + nn + nnn. Например, пользователь ввёл число 3. Считаем 3 + 33 + 333 = 369.

n = int(input("Введите число n: "))
m = (str(n))
m1 = m + str(n)
m2 = m + str(n) + str(n)
print("Сумма n+nn+nnn= ", int(m) + int(m1) + int(m2))

# 4. Пользователь вводит целое положительное число. Найдите самую большую цифру в числе. Для решения используйте цикл while и арифметические операции.

a = int(input("Введите целое положительное число: "))
m = a%10
a = a//10
while a > 0:
    if a%10 > m:
        m = a%10
    a = a//10
print("Самая большая цифра в числе:", m)

# 5. Запросите у пользователя значения выручки и издержек фирмы. Определите, с каким финансовым результатом работает фирма (прибыль — выручка больше издержек, или убыток — издержки больше выручки). Выведите соответствующее сообщение. Если фирма отработала с прибылью, вычислите рентабельность выручки (соотношение прибыли к выручке). Далее запросите численность сотрудников фирмы и определите прибыль фирмы в расчете на одного сотрудника.

a = float(input("Введите значение выручки фирмы: "))
b = float(input("Введите значение издержек фирмы: "))
if a > b:
    print(" У фирмы положительный финансовый результат! Прибыль составляет", round(a - b, 2), "руб.", '\n', "Рентабельность выручки составляет: ", round((a - b) / a, 2), "%")
    c = int(input(" Введите количество сотрудников фирмы: "))
    print(" Прибыль фирмы в расчёте на 1 сотрудника составляет", round((a - b) / c, 2), "руб.")
else:
    print(" У фирмы отрицательный финансовый результат! Убыток составляет", round(a - b, 2), "руб.")

# 6. Спортсмен занимается ежедневными пробежками. В первый день его результат составил a километров. Каждый день спортсмен увеличивал результат на 10 % относительно предыдущего. Требуется определить номер дня, на который результат спортсмена составит не менее b километров. Программа должна принимать значения параметров a и b и выводить одно натуральное число — номер дня.

a = float(input("Введите результат спортсмена в 1й день (км.): "))
b = float(input("Введите расстояние для расчёта дня (км.): "))
day = 1
while b-a>0:
    a=a+(a*0.1)
    day += 1
    print(day,"день:", round(a,2),"км.")
